# FC Reorder v1.8 verification

Recorded 2026-09-04 on branch `fc-reorder-dashboard`. Browser walkthrough was skipped by request; this file captures automated evidence only.

## Commands

| Check | Command | Result |
| --- | --- | --- |
| Reorder tests | `npx vitest run tests/reorder` | 38 files, then e2e added; see latest run below |
| Typecheck | `npx tsc --noEmit` | PASS |
| Whitespace | `git diff --check` | Run with the current working tree |

## Automated results

- Reorder Vitest suite: **39 files / 184 tests PASS** after Task 15–16.
- Shared metric engine: Overview, Analytics, and CSV export return the same MS/MD/MSI/MGO/NO values (`tests/reorder/reorder-e2e.test.ts`).
- Missing coverage stays `null` / `—`, not a fabricated zero.
- Tenant isolation, FC Ops API key, public `Cache-Control: private, no-store`, upload limits, and claim-code encryption are covered by `tests/reorder/security.test.ts` and `tests/reorder/claim-code-security.test.ts`.
- Existing Dashboard static host remains `src/dashboard` at `/`; Reorder is `src/reorder-dashboard` under `/reorder/*`; legacy consumer tap remains `/tap/{SN}`.

## Browser / accessibility

Not re-run in this pass. Prior notes still apply:

- Original `/` Dashboard browser check: 2026-09-03
- Data Source import UI at 375/430/1440: 2026-09-04

Static contracts now asserted:

- Interactive targets `min-height: 44px` under `.reorder-app`
- `:focus-visible` ring
- `prefers-reduced-motion`
- No `text-transform: uppercase`
- Single page `<h1>`

Viewport matrix 375 / 390 / 430 / 768 / 1024 / 1440 remains pending a later browser pass.

## Known environment limitations

| Item | Safe shipped behavior | Unblock |
| --- | --- | --- |
| Native `reorder_fc_event` table / FC event API | MSI is `unavailable` / `—` when FC event coverage is missing | Add event ingest table + `tests/reorder/fc-event-api.test.ts` |
| Numeric dwell-time threshold for MSI | Meaningful-action filter excludes bot/staff/rapid repeats | Product supplies a duration |
| 2026 Seller Central Coupon template | Interim 2025 schema parser with unknown-column acknowledgement | Real template fixture |
| Activation scheduler in the deployed app | Migration exists; runner is API-key protected | Deploy this branch’s `/api/internal/reorder/jobs/activate-due` |
| Claim-code encryption key | `REORDER_CLAIM_CODE_KEY` or `API_KEY`; production refuses to start encrypting without one | Set the secret before importing new Single-use codes |

## Happy path covered by tests (not live HTTP)

Established FC Order → allocation lock → FC Ops Batch/FC IDs → Product/discount/Survey → publish validation → consumer allocate/reveal → four source facts → Overview/Analytics/export agreement.
