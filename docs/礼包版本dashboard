# Brand Dashboard PRD｜收入优先版

## 1. 页面目标

Brand Dashboard 用来帮助品牌判断这套挑战机制是否带来真实经营收益。

品牌最关心的是：

1. 挑战机制带来了多少收入。
2. 优惠券带来了多少收入。
3. 复购用户贡献了多少收入。
4. 活跃用户的人均收入是否提升。
5. 哪些优惠券真正带来了订单和收入。
6. 这套机制是否提升了复购、留存和回流。

后台不展示游戏过程、金币规则、任务细节。品牌只看最终经营结果。

---

## 2. 页面结构

Dashboard 分为 6 个模块：

1. 时间筛选
2. 收入概览
3. 物理触点表现（Physical Touchpoint Performance）
4. 优惠券收入漏斗
5. 优惠券表现
6. 收入趋势

---

## 3. 时间筛选

页面顶部提供时间筛选。

默认展示：

* Last 30 days

可切换：

* Last 7 days
* Last 30 days
* Last 90 days
* Month to date
* All time

时间筛选影响整个 Dashboard 的所有数据。

---

## 4. 收入概览

收入概览是首屏最重要的区域。

这里集中展示品牌最关心的收入、复购、留存、回流结果。

### 4.1 首屏指标

| 指标                                            | 含义                        |
| --------------------------------------------- | ------------------------- |
| Challenge attributed revenue / Coupon revenue | 挑战机制带来的总归因收入 / 使用优惠券产生的收入 |
| Repeat customer revenue                       | 复购用户贡献的收入                 |
| Revenue per magnet / Active magnets           | 每个活跃 magnet 平均带来的收入 / 活跃 magnet 数 |
| Revenue growth                                | 当前周期相较上一周期的收入增长           |
| Repeat purchase rate                          | 当前周期复购率                   |
| 30-day retention                              | 当前周期 30 日留存               |
| Winback rate                                  | 当前周期回流用户比例                |

### 4.2 展示规则

收入类指标优先展示在第一行：

| 第一行指标                                         | 说明         |
| --------------------------------------------- | ---------- |
| Challenge attributed revenue / Coupon revenue | 最核心收入结果    |
| Repeat customer revenue                       | 复购收入       |
| Revenue per magnet / Active magnets           | 人均收入与活跃规模  |
| Revenue growth                                | 收入增长       |

复购、留存、回流指标展示在同一个 Revenue Overview 模块的第二行：

| 第二行指标                | 说明         |
| -------------------- | ---------- |
| Repeat purchase rate | 判断是否提升复购   |
| 30-day retention     | 判断是否提升留存   |
| Winback rate         | 判断是否唤回沉默用户 |

### 4.3 合并展示规则

如果两个指标表达的是同一类经营结果，可以共同存在于一个卡片里，用斜杠区分，不拆成两个重复卡片。

示例：

| 卡片                                            | 展示方式                    |
| --------------------------------------------- | ----------------------- |
| Challenge attributed revenue / Coupon revenue | 上方展示挑战归因总收入，下方展示其中优惠券收入 |
| Revenue per magnet / Active magnets           | 上方展示人均收入，下方展示活跃 magnet 数（去重领券用户数） |

### 4.4 不放在收入概览里的指标

以下指标不放首屏收入概览：

* Active devices
* Participants
* Coupons earned
* Coupons used
* Orders 单独卡片
* Use rate
* Completion rate
* Game plays
* Coins earned
* Task completions
* Survey responses

这些数据只用于解释收入来源，不作为首屏收入指标。

### 4.5 指标计算规则提示（info tooltip）

读者是使用我们系统的品牌方，不一定清楚每个指标怎么算。因此每个指标卡片都要让品牌随手看到它的计算规则。

规则：

* 每个指标卡片标题右上角带一个小 info 图标（ⓘ）。
* 鼠标 hover（或点击 / 键盘聚焦，便于移动端与无障碍）即弹出该指标的计算规则说明，用平实的英文描述口径，面向品牌方而非工程师。
* 暂无数据源的指标（pending），提示文案需明确写明「No data source yet — pending instrumentation」，避免品牌误以为是 0。
* 本规则适用于所有指标卡片，包括 Revenue Overview 与 Physical Touchpoint Performance 两个模块。

各指标的提示文案即本 PRD 中对应的「计算规则」口径（见 §4.1 与 §5.2）。

---

## 5. 物理触点表现（Physical Touchpoint Performance）

### 5.1 模块目标

收入概览回答「赚了多少钱」，物理触点表现回答「冰箱贴这个物理入口被用得怎么样」。

它衡量 magnet（冰箱贴）作为线下物理入口的：被触达规模、触达频率、停留质量。用于判断 magnet 是「一次性流量」还是「能沉淀下来的习惯入口」。

位置：放在 Revenue Overview 下方、优惠券收入漏斗上方。

### 5.2 指标与计算规则

模块为 3 个核心卡片。Daily / Weekly / Monthly 不单独成卡，作为 Magnet frequency 卡片底部的一行分解展示：

| 卡片 | 字段 | 计算规则 | 回答的问题 |
| ---- | ---- | -------- | --------- |
| Magnet exposure（物理触达量） | `magnetExposure` | 时间段内 magnet 被触发 / 激活的总次数 = `count(tap events)`。每次 NFC / 扫码打开页面计 1 次 | 有多少物理入口被用起来 |
| Magnet frequency（触达频率） | `magnetFrequency` | 每台设备平均触达次数 = 总触达次数 ÷ 去重设备数 = `count(tap events) / count(distinct magnet_id)`。卡片底部按日 / 周 / 月分解展示 | 是一次性流量还是习惯入口 |
| Magnet dwell time（平均停留时间） | `magnetDwellTime` | 每次触发后页面平均停留时长（秒）= `sum(session_duration) / count(sessions)`；前端展示自动转 `45s` / `1m 20s` | 用户有没有被内容留住 |

Magnet frequency 卡片底部分解（同一份 tap event 埋点，仅按时间粒度归一，不是独立指标）：

| 分解项 | 字段 | 计算规则 |
| ---- | ---- | -------- |
| Daily | `interactionsDaily` | 每台设备日均触达 = 总触达次数 ÷ 去重设备数 ÷ 周期天数 |
| Weekly | `interactionsWeekly` | 每台设备周均触达 = 日均 × 7（或按自然周聚合） |
| Monthly | `interactionsMonthly` | 每台设备月均触达 = 日均 × 30（或按自然月聚合） |

### 5.3 数据源依赖（埋点需求）

> **当前状态：以上 6 个指标尚无数据源，前端统一以 `—` + 「No data source」占位展示（pending）。** 待埋点接入后由后端填充 `BrandDashboard.touchpoints` 即可点亮。

仓库现有数据无法支撑本模块：`magnet` 表只是注册表（`id, customer_id, sn, url, role, stage`），不含触达 / 停留事件；唯一与 magnet 相关的活动是扫码发券（落在 `fc_coupon_assignment`），只反映「发券」，不反映「曝光 / 频率 / 停留」。

需要新增一张 magnet 触达事件表，建议字段：

| 字段 | 说明 |
| ---- | ---- |
| `magnet_id` | 触发的 magnet（设备）主键 |
| `customer_id` | 所属品牌 |
| `ts` | 触发时间戳（用于按时间筛选与日 / 周 / 月归一） |
| `session_id` | 单次会话标识（用于去重与停留计算） |
| `dwell_seconds` | 本次会话停留时长（进入页面记开始，离开 / 心跳记结束）→ 支撑 Magnet dwell time |

有了「每次触达一行」的事件表，除 dwell time 需额外的停留时长字段外，其余 5 个指标均可由该表聚合得出。

### 5.4 后端返回结构（待实现）

```ts
interface BrandDashboardTouchpoints {
  magnetExposure: number | null;      // 总触达次数
  magnetFrequency: number | null;     // 每设备平均触达
  magnetDwellTime: number | null;     // 平均停留秒数
  interactionsDaily: number | null;   // 每设备日均
  interactionsWeekly: number | null;  // 每设备周均
  interactionsMonthly: number | null; // 每设备月均
}
// 挂在 BrandDashboard.touchpoints；无数据源时各字段返回 null → 前端 pending
```

---

## 6. 优惠券收入漏斗

### 6.1 模块目标

优惠券收入漏斗用来解释收入是怎么来的。

首屏先告诉品牌赚了多少钱，漏斗再解释：

这些收入来自多少设备、多少参与用户、多少优惠券、多少订单。

### 6.2 漏斗阶段

| 阶段             | 含义                |
| -------------- | ----------------- |
| Active devices | 当前时间段内处于激活状态的设备数量 |
| Participants   | 参与挑战的用户           |
| Coupons earned | 获得优惠券             |
| Coupons used   | 使用优惠券             |
| Orders         | 使用优惠券产生的订单        |
| Coupon revenue | 使用优惠券产生的收入        |

### 6.3 Active devices 规则

Active devices 是漏斗的起点，用来告诉品牌：

当前时间段内有多少设备正在产生有效触点。

它不是用户行为指标，也不是收入指标，所以不放在 Revenue Overview 里。

Active devices 用来帮助品牌判断：

| 现象                                | 说明               |
| --------------------------------- | ---------------- |
| Active devices 多，但 Participants 少 | 设备触点存在，但用户参与挑战不足 |
| Active devices 少，但收入高             | 设备覆盖少，但单设备质量高    |
| Active devices 多，收入低              | 设备覆盖足够，但后续转化弱    |

### 6.4 展示规则

漏斗每一层展示：

* 数量
* 相对上一层的转化率
* 相对 Active devices 的总转化率

其中 Coupon revenue 是金额结果，不需要展示转化率，只展示收入金额。

### 6.5 关键规则

获得优惠券就代表用户已经拿到 coupon code。

不展示：

* Coupon revealed
* Saved coupon
* Scratch code
* Reveal code

旧版漏斗：

| 旧阶段          | 处理               |
| ------------ | ---------------- |
| Saw coupon   | 删除               |
| Saved coupon | 删除               |
| Used coupon  | 保留为 Coupons used |

新版漏斗：

| 新阶段            | 说明       |
| -------------- | -------- |
| Active devices | 当前周期激活设备 |
| Participants   | 参与挑战用户   |
| Coupons earned | 获得优惠券    |
| Coupons used   | 使用优惠券    |
| Orders         | 用券订单     |
| Coupon revenue | 用券收入     |

### 6.6 运营判断

| 现象                                | 说明                      |
| --------------------------------- | ----------------------- |
| Active devices 高，Participants 低   | 用户看到入口但没有参与挑战           |
| Participants 高，Coupons earned 低   | 用户参与了挑战，但没有有效获得券        |
| Coupons earned 高，Coupons used 低   | 用户拿到券后没有足够动力下单          |
| Coupons used 高，Orders 低           | 可能订单归因或 checkout 链路存在问题 |
| Orders 高，Coupon revenue 低         | 可能客单价低，或高折扣券占比过高        |
| Active devices 高，Coupon revenue 低 | 设备规模足够，但整体转化效率不足        |

---

## 7. 优惠券表现

### 7.1 模块目标

让品牌知道哪一档优惠券真正带来了收入。

### 7.2 表格维度

| Coupon  | Earned | Used | Orders | Revenue | Use rate |
| ------- | -----: | ---: | -----: | ------: | -------: |
| 10% OFF |    320 |   78 |     72 |  $3,120 |      24% |
| 15% OFF |    188 |   62 |     58 |  $2,460 |      33% |
| 20% OFF |     92 |   45 |     41 |  $1,980 |      49% |
| 25% OFF |     31 |   19 |     18 |    $920 |      61% |

### 7.3 排序规则

默认按照优惠力度从低到高展示。

### 7.4 品牌可以判断

1. 哪一档券带来的收入最高。
2. 哪一档券带来的订单最多。
3. 哪一档券使用率最高。
4. 高折扣券是否真的比低折扣券更有效。
5. 哪一档券可能需要调整门槛或有效期。

---

## 8. 收入趋势

### 8.1 模块目标

展示当前时间段内收入变化。

### 8.2 展示内容

收入趋势优先展示：

1. Challenge attributed revenue
2. Coupon revenue

可以叠加展示：

1. Coupons used
2. Orders

图表主轴必须是收入，不以用券数作为主指标。

### 8.3 目的

帮助品牌判断：

* 收入是否在增长
* 哪些时间段收入高
* 哪些时间段优惠券使用带来了明显收入
* 最近是否出现收入下降

---

## 9. 旧版 Dashboard 数据处理

旧版音频 / CTA Dashboard 中的数据按以下规则处理。

### 9.1 保留并前置到 Revenue Overview

| 旧版指标                    | 新版处理                                                    |
| ----------------------- | ------------------------------------------------------- |
| FC Attributed Revenue   | 改为 Challenge attributed revenue，放入收入概览                  |
| Coupon Redeemed Revenue | 改为 Coupon revenue，和 Challenge attributed revenue 放在同一卡片 |
| Repeat Customer Revenue | 保留为 Repeat customer revenue，放入收入概览                      |
| Revenue / Active HH     | 改为 Revenue per magnet / Active magnets，放入收入概览（计算口径不变：Coupon revenue ÷ 去重领券用户数） |
| Repeat Purchase Rate    | 保留为 Repeat purchase rate，放入收入概览第二行                      |
| 30-day Retention        | 保留为 30-day retention，放入收入概览第二行                          |
| Winback Rate            | 保留为 Winback rate，放入收入概览第二行                              |

### 9.2 删除

| 旧版指标                        | 处理  |
| --------------------------- | --- |
| C1 Sticking                 | 不保留 |
| C2 Regular Tap              | 不保留 |
| C3 Weekly Tap               | 不保留 |
| C4 CTA Click                | 不保留 |
| C5 Take                     | 不保留 |
| Saw coupon                  | 不保留 |
| Saved coupon                | 不保留 |
| Coupon revealed             | 不保留 |
| CTA Impressions             | 不保留 |
| Revenue / CTA Click         | 不保留 |
| CTA Performance             | 不保留 |
| Source / Magnet Performance | 不保留 |

---

## 10. 空状态

### 10.1 没有任何活动

No activity yet.
Once devices become active and users join the challenge, revenue and coupon data will appear here.

### 10.2 有激活设备但没有参与

Devices are active, but no users have joined the challenge yet.

### 10.3 有参与但没有收入

Users have started joining the challenge, but no coupon revenue has been generated yet.

### 10.4 有获得券但没有使用

Coupons have been earned, but none have been used yet.

### 10.5 Shopify 未连接

Connect Shopify to track coupon usage, orders, and revenue.

未连接 Shopify 时，不展示订单和收入假数据。

---

## 11. 页面操作

### 11.1 Export

保留 Export。

导出内容包括：

* 时间范围
* 激活设备数
* 参与用户数
* 优惠券档位
* 获得数量
* 使用数量
* 订单数
* 收入
* 使用率

### 11.2 View orders

Shopify 已连接时，可以从订单数字进入订单明细。

订单明细展示：

* 订单号
* 用户
* 使用的优惠券
* 订单金额
* 下单时间
* 订单状态

---

## 12. 本期范围

本期必须完成：

1. 时间筛选
2. 收入概览
3. 优惠券收入漏斗
4. 优惠券表现表
5. 收入趋势
6. Export
7. Shopify 未连接降级状态
8. 空状态

本期不做：

1. 游戏数据
2. 金币数据
3. 任务完成率
4. 问卷细节
5. Coupon revealed
6. Saved coupon
7. 旧版 C1-C5 漏斗
8. 技术同步状态
9. Klaviyo sync status
10. 用户分群
11. Source / Magnet Performance
12. Discount cost
13. Net revenue
14. Refund analysis
15. CTA Performance

---

## 13. 最终页面信息架构

新版 Brand Dashboard 最终结构为：

1. Date Filter

2. Revenue Overview

   * Challenge attributed revenue / Coupon revenue
   * Repeat customer revenue
   * Revenue per magnet / Active magnets
   * Revenue growth
   * Repeat purchase rate
   * 30-day retention
   * Winback rate

3. Physical Touchpoint Performance

   * Magnet exposure
   * Magnet frequency（底部分解：Daily / Weekly / Monthly）
   * Magnet dwell time

4. Coupon Revenue Funnel

   * Active devices
   * Participants
   * Coupons earned
   * Coupons used
   * Orders
   * Coupon revenue

5. Coupon Performance

   * By coupon tier

6. Revenue Trend

   * Challenge attributed revenue
   * Coupon revenue

---

## 14. 验收标准

1. 首屏必须优先展示收入相关指标。
2. Repeat purchase rate、30-day retention、Winback rate 必须合并展示在 Revenue Overview，不再单独成模块。
3. Coupon Revenue Funnel 第一层必须是 Active devices。
4. Participants、Coupons earned、Coupons used 不作为首屏核心概览卡片。
5. Dashboard 不出现游戏、金币、任务细节。
6. Dashboard 不出现 Coupon revealed、Saved coupon、Scratch code。
7. 同一经营含义的收入指标可以在同一卡片内用斜杠合并展示。
8. 同一个指标不能在多个模块重复作为独立卡片展示。
9. 品牌可以在首屏看到挑战归因收入、优惠券收入、复购收入、人均收入、复购率、留存和回流。
10. 品牌可以在漏斗中看到 Active devices → Participants → Coupons earned → Coupons used → Orders → Coupon revenue。
11. 品牌可以按优惠券档位查看 earned、used、orders、revenue、use rate。
12. 品牌可以看到收入趋势。
13. 未连接 Shopify 时，不展示订单和收入假数据，只展示连接提示。
14. Physical Touchpoint Performance 模块展示在 Revenue Overview 下方、Coupon Revenue Funnel 上方；其指标在埋点接入前以 `—` + 「No data source」占位，不展示假数据。
15. 每个指标卡片右上角提供 info 图标，hover / 点击可查看该指标的计算规则，文案面向品牌方（见 §4.5）。
